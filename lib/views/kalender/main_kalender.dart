import 'package:flutter/material.dart';

class KalenderCutiPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Kalender Cuti'),
      ),
      body: Center(
        child: Text('Ini adalah halaman Kalender Cuti'),
      ),
    );
  }
}
